<%= form_for(@label) do |f| %>
  <% if @label.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@label.errors.count, "error") %> prohibited this label from being saved:</h2>

      <ul>
      <% @label.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name, class: :styled_input %>
  </div>
  <div class="field">
    <%= f.label :location %><br>
    <%= f.text_field :location, class: :styled_input %>
  </div>
  <div class="actions">
    <%= f.submit(class: "pretty_button") %>
  </div>
<% end %>
